node_modules/
dist/

# Entornos — contienen credenciales, no subir al repo
.env.development
.env.production
# .env.example sí se puede commitear (no tiene credenciales reales)

# Export de datos de Firebase usado para la migración (contiene datos reales)
*-rtdb-export.json
firebase-export.json
