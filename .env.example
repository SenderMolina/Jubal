# Copia este archivo como .env.development (para local) o .env.production (para deploy)
# y completa con las credenciales del proyecto Supabase correspondiente.
# (Settings → API en el dashboard de Supabase)

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
